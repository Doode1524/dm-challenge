# dm-challenge

# In your terminal, run "bundle install" to build the app.
    - bundled with bundler version 2.1.4
    - follow the prompts in the terminal to handle any compatibility issues at this point. 

# To run the program, "ruby bin/run" in your terminal.
    - ruby version 2.6.1
    - rvm version 1.29.12

# Run "rspec" in terminal to run all the tests.
    - testing with rspec version 3.10.0
